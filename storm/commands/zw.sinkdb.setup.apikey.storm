init { $privsep = $lib.import(zw.sinkdb.privsep) }

if ($cmdopts.self) {
    $lib.print("Setting SinkDB HTTPS API key for the current user.")
} else {
    $lib.print("Setting SinkDB HTTPS API key for all users.")
}

$privsep.setApiKey($cmdopts.apikey, $cmdopts.self)